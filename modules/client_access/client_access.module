<?php

/**
 * Implements hook_ctools_plugin_directory().
 *
 * Informs Ctools module a new plugin is available
*/
function client_access_ctools_plugin_directory($module, $plugin){
	if($module=='ctools' && !empty($plugin)){
		return "plugins/$plugin";
	}
}
